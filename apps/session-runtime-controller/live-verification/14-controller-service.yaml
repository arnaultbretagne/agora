apiVersion: v1
kind: Service
metadata:
  name: session-runtime-controller
  namespace: agora-p04-test
spec:
  selector:
    app: session-runtime-controller
  ports:
    - port: 8443
      targetPort: 8443
