# The operator-managed ConfigMap `pod-spec.ts` mounts by name (never accepted from the
# materialize request). Value matches `relay-bundle.ts`'s `fakeRelayBundle()` exactly — P08
# replaces the value, never where/how the Pod refers to it.
apiVersion: v1
kind: ConfigMap
metadata:
  name: agora-onecli-ca
  namespace: agora-p04-test
data:
  ca.pem: |
    -----BEGIN CERTIFICATE-----
    FAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKEFAKE
    This is a placeholder, non-functional CA for P04 development only.
    P08 replaces this bundle with the real operator-managed OneCLI CA.
    -----END CERTIFICATE-----
