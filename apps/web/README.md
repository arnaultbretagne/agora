# `@agora/web`

Human-facing Workstream UI. It consumes the product HTTP API and resumable feed; it never connects
directly to a Session Runtime, ACP Agent, Broker, or custody store.

Implementation is governed by `plans/05-web-and-projections.md`.
