{
  "name": "@agora/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./dist/src/server.js",
  "scripts": {
    "build": "tsc -p . && tsc -p src/client",
    "typecheck": "tsc -p . --noEmit && tsc -p src/client --noEmit",
    "start": "node dist/src/main.js",
    "test": "node --test \"dist/test/**/*.test.js\""
  }
}
