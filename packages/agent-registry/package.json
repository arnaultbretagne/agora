{
  "name": "@agora/agent-registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./dist/src/index.js",
  "scripts": {
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit",
    "test": "npm run build && node --test $(find dist -name '*.test.js')"
  },
  "dependencies": {
    "@agora/session-runtime-control": "0.0.0",
    "ajv": "8.20.0"
  }
}
