# `@agora/acp`

Thin integration around the official ACP v1 SDK: connection lifecycle, host callbacks, complete
envelope observation and correlation with durable product commands.

It must not define a normalized semantic protocol that competes with ACP.

Implementation is governed by `plans/03-acp-session-coordinator.md`.
