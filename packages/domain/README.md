# `@agora/domain`

Dependency-light domain types, commands and invariant checks for Workstreams, Sessions and Anchors.
It contains no Postgres, HTTP, Kubernetes, ACP transport or harness code.

Implementation is governed by `plans/01-domain-and-contracts.md`.
