# `@agora/custody`

Opaque, append-only custody snapshot storage. Only runtime identities may stream payload bytes;
product identities can read snapshot metadata.

Implementation is governed by `plans/06-custody-and-resume.md`.
