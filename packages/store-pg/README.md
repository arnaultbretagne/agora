# `@agora/store-pg`

Postgres repositories for product facts, the canonical Workstream event journal, reliable journal
notifications, per-Workstream projector checkpoints, durable feed positions and rebuildable
projections.

Implementation is governed by `plans/02-postgres-store.md`.
