# `@agora/equipment-policy`

Resolves user-visible resource intents into auditable capability facts and opaque execution grants.
It owns combination validation and deterministic OneCLI route-policy input; the Browser and Session
Runtime never compose raw claims or OneCLI rules.

Implementation is governed by `plans/08-equipment-and-broker.md`.
